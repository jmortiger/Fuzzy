name: fuzzy
description: "An all-in-one mobile frontend for multiple image submission sites."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  archive: ^3.6.1
  flutter:
    sdk: flutter
  http: ^1.2.0
  j_util:
    #path: C:/Users/***REMOVED***source/repos/j_util
    path: ../j_util/
  oauth2: ^2.0.2
  package_info_plus: ^5.0.1
  path_provider: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  test: ^1.24.9

flutter:
  uses-material-design: true
